# Nombre de la aplicación
spring.application.name=dontar_demo

# Configuración de la base de datos MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/clinica_odontologica
spring.datasource.username=Camila
spring.datasource.password=Camila38944251
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# Configuración de JPA (opcional)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Configuración del servidor (si deseas cambiar el puerto por defecto)
server.port=8081

# Habilitar el modo de devtools (opcional para desarrollo)
spring.devtools.restart.enabled=true

